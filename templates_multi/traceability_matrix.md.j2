# Matrice de Traçabilité

## 🎯 Objectifs → Exigences → Solutions

| Objectif Business | Exigences | Applications | Risques |
|-------------------|-----------|--------------|---------|
{% for obj in objectives %}
| {{ obj.Objective }} | {{ obj.KPI }} ≥ {{ obj.Target }}{{ obj.Unit }} | {% for app in applications if app.Domain in obj.Objective %}{{ app.Name }}{% if not loop.last %}, {% endif %}{% endfor %} | Performance |
{% endfor %}

## 📊 Applications → Processus → Données

| Application | Processus Supportés | Données Manipulées | Criticité |
|-------------|--------------------|--------------------|-----------|
{% for app in applications %}
| **{{ app.Name }}** | {% for proc in businessprocesses %}{% if app.ID in flows | selectattr('BusinessProcess', 'equalto', proc.Name) | map(attribute='Outbound') | list %}{{ proc.Name }}{% if not loop.last %}, {% endif %}{% endif %}{% endfor %} | {% for data in data_entities %}{{ data.Entity }}{% if not loop.last %}, {% endif %}{% endfor %} | {{ app.Security_Level or 'Medium' }} |
{% endfor %}

## 🔐 Flux → Zones → Contrôles

| Flux | Zone Source → Cible | Protocole | Contrôles Sécurité |
|------|---------------------|-----------|---------------------|
{% for flow in flows %}
| {{ flow.Outbound }} → {{ flow.Inbound }} | {% for app in applications if app.ID == flow.Outbound %}{{ app.Network_Zone }}{% endfor %} → {% for app in applications if app.ID == flow.Inbound %}{{ app.Network_Zone }}{% endfor %} | {{ flow.Protocol }} | {{ flow.Security_Controls or 'Standard' }} |
{% endfor %}

## 📋 Conformité & Standards

| Exigence | Standard | Applications Concernées | Statut |
|----------|----------|-------------------------|---------|
| PCI-DSS | Payment security | {% for app in applications if app.Network_Zone == 'PCI' %}{{ app.Name }}{% if not loop.last %}, {% endif %}{% endfor %} | 🟡 À valider |
| GDPR | Data privacy | {% for data in dataclass if data.Classification == 'PII' %}{{ data.Entity }}{% if not loop.last %}, {% endif %}{% endfor %} | 🟢 Conforme |
| SOX | Financial controls | {% for app in applications if app.Department == 'Finance' %}{{ app.Name }}{% if not loop.last %}, {% endif %}{% endfor %} | 🟡 En cours |